using UnityEngine;

public class WindowUI : MonoBehaviour
{
    [SerializeField] private string windowTitle = "Title";
    [SerializeField] private bool isResizable = true;
    [SerializeField] private Vector2 minSize = new Vector2(200, 100);
    [SerializeField] private Vector2 maxSize = new Vector2(800, 600);
    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
